m_Camera->setProjectionType(Ogre::PT_ORTHOGRAPHIC);
    Ogre::Real aspect = (Ogre::Real)mViewport->getActualWidth() / (Ogre::Real)m_Viewport->getActualHeight();
    m_Camera->setAspectRatio(aspect);
    m_Camera->setNearClipDistance(1);
    m_Camera->setOrthoWindow(aspect * 2, 2);
